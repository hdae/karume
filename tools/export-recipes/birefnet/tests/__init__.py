"""BiRefNet 系 recipe のテスト。

パッケージにしてあるのは、family が増えたときに `<family>/tests/test_export.py` の
**basename が衝突しない**ようにするため（pytest の既定 import mode は `__init__.py` の
無いディレクトリのテストを basename だけのモジュール名で読む）。
"""
