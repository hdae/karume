# 性能台帳（perf-ledger）

> 未消化の性能候補の**判断と順序**を持つ常設台帳。実測値の正本は `docs/research/` の各
> スナップショット — 本台帳は数値を写さず出典を指す（数値を書く場合は時点を必ず添える）。
> **優先順位の正本はこの 1 本**（ACTIVE_DESIGN からは参照のみ・二重台帳を作らない）。
> 起票の出所: 2026-08-12 網羅レビュー D4 レッグ（`.claude/reviews/2026-08-12_faef828/findings/D4.md`
> — git 追跡外のため判断部を本台帳へ移した。旧 `.claude/reviews/PERFORMANCE.md` は時点
> スナップショットとして凍結）。H-5 / H-6 / K-8 / K-9 / L-4〜L-6 の出所は外部レビュー
> 2026-08-13（`.claude/reviews/2026-08-13_chatgpt-reviews/` — git 追跡外のため、判断に要る
> 数値は時点付きで本台帳へ転記した）。

## 実施順（2026-08-12 裁定）

1. **第 1 段（前提ゼロ）**: Irodori 融合カウント門（済 — 網羅レビュー修正波）・現行 HEAD での
   op 別 GPU 内訳の再取得（**済 2026-08-13** —
   [op-timing-restats](research/2026-08-13-op-timing-restats.md)。K-8 は同実測で棄却）・
   docs の失効比率注記（済）。
2. **第 2 段（ホスト境界）**: H-2 → H-1 / H-3。H-4 はタスク #7（Session 常駐 opt-in）と同体。
3. **第 3 段（数値契約を割る群）**: K-2 / K-4 / K-5 は **PNG/WAV sha256 門の代替品質門の設計が
   先行条件**（ADR 0022 の規律の外に出る — 品質門なしの着手は禁止）。

## ホスト境界（run あたり固定費）

| ID  | 候補                                                                                   | 根拠（出典）                                                                                                                                                                                                                                                                                                                 | 前提・リスク                                                                                                                                                                                        |
| --- | -------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| H-1 | run の二段待ち（compute 完了待ち → copy submit → mapAsync）を単一完了待ちへ            | EG per-run 固定費 ~38ms・同一 GPU の ORT Web ~20ms が上限の存在証明（[ort-comparison](research/2026-08-11-embeddinggemma-ort-comparison.md) §1/§6）                                                                                                                                                                          | flush の onSubmittedWorkDone は適応チャンク推定の窓 + timestamp 回収と結合（submit.ts の #closeMeasurementWindow）— 単一化は TDR 予算推定の再設計とセット。kill 基準: EG bare 53.8 → 43.8ms 未満    |
| H-2 | ~38ms の分解（Deno per-call 費用 vs 同期待ち・Chrome 対照）                            | dispatch −456 本で wall 中立という負の実測（ACTIVE_DESIGN / ort-comparison §6）                                                                                                                                                                                                                                              | 分解自体は利得ゼロ — H-1/H-3 の採否根拠。device メソッドラップ手法は op-timing-stats §5 に既存                                                                                                      |
| H-3 | 入力の内容アドレスキャッシュ（params キャッシュの入力版）                              | Irodori DiT が定数条件 3 本を毎 forward 再アップロード — 実配布形の実値で 3.70MiB/forward × 40〜100 = **155〜388MB/生成**（レビュー検証レッグの再計算・未実測）                                                                                                                                                              | 黙って飛ばすのは沈黙誤値の温床 — 飛ばした事実を診断へ出す形が前提。HOST-006 の params 系は約半分 GPU 重畳（op-timing-stats §7）で**利得ゼロの可能性あり・実測が先**                                 |
| H-4 | pipeline 単位の Session 常駐（residency + warmup）                                     | SBV2 はホスト律速（壁 1.08s vs GPU 0.42s）・Session 構築 gap 2.50s（op-timing-stats §9）                                                                                                                                                                                                                                     | タスク #7（保留）と同体。VRAM 常駐増・device loss 再構築規律が要る。LLM decode（perf 外の必須前提）とも接続 — ADR 0047 決定 3                                                                       |
| H-5 | Irodori/Anima の反復状態 GPU 常駐（latent / CFG / Euler を GPU に置き run 境界を消す） | 各 predict が velocity を CPU へ readback → host CFG/Euler → 再アップロード × 40〜100 forward。2026-08-13 実測: irodori の露出ホスト ≈5.3s（装置代補正後・壁の ≈4 割 — [op-timing-restats](research/2026-08-13-op-timing-restats.md) §3.3）。staged execution の棄却（encoder-only 0.60% / timestep-only 0.12%）とは**別軸** | prepared backing の出力は次 run で上書きされる — resident handle / ping-pong / 出力寿命 pin の runtime API 設計が要る（着手時に設計案を先出し）。効果は H-1 の固定費と同根なので H-1 とセットで実測 |
| H-6 | Anima tiled VAE の内部 clone 層削除（→ 将来 row streaming）                            | `assembleTiles()` の public non-mutating 契約により内部 owner 経路でも全 tile を複製 — 1024px で 27+27MiB / 2048px で 75+75MiB（2026-08-13 静的算出）                                                                                                                                                                        | 公開 API の non-mutating 契約は維持し、内部 owner 用の owned assembly だけ追加する。blend 順序不変（ビット一致）が門                                                                                |

## カーネル（GPU 時間）

| ID   | 候補                                                              | 根拠（出典）                                                                                                                                                                                                                                                | 前提・リスク                                                                                                                                                                                                                                                                                       |
| ---- | ----------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| K-1  | DiT linear i8a8 + attention の次の道具                            | GPU 支配項 — 2026-08-13 再実測: linear i8a8 45.0% / attention 3 種 20.7% / VAE 16.3%（全 GPU 7.93s 比・[op-timing-restats](research/2026-08-13-op-timing-restats.md) §1）                                                                                   | 幾何掃引 1 巡は消化済み・大きい利得は subgroup 等 portable でない道具が要る                                                                                                                                                                                                                        |
| K-2  | VAE conv2d の Winograd / i8a8                                     | VAE 1.31s（10.5s 時点で GPU 比 16.3% — op-timing-stats §9）                                                                                                                                                                                                 | **PNG sha256 門が割れる** — 画像品質門の新設が先行条件。kill 1.3 倍未満                                                                                                                                                                                                                            |
| K-3  | i8a8 L2 形状バケット（r16×8）                                     | ×1.06 相当（[kernel-variant-sweep](research/2026-08-10-kernel-variant-sweep.md) §2）                                                                                                                                                                        | i8a8-geometry が「キー本数と検証面が倍」で明示見送り済み — **採らないが記録**                                                                                                                                                                                                                      |
| K-4  | SBV2 conv1d/convT の専用化                                        | front+voice の GPU 96.7%（op-timing-stats §4）                                                                                                                                                                                                              | SBV2 の律速はホスト — H-4 の後でないと E2E 採否が測れない。i8a8 化は WAV 門を割る。具体技法候補: conv1d は implicit GEMM（K flatten を ic→k 昇順に保てばビット一致余地）・convT は residue grouping（k ≡ ox+padding (mod stride) — 加算順不変のまま % / 無効 tap を除去。2026-08-13 外部レビュー） |
| K-5  | Irodori DiT の masked online attention                            | S=750 の scores 129.8MiB が core 既定 128MiB を超える（[dacvae-codec-recon](research/2026-08-12-dacvae-codec-recon.md)）。2026-08-13 実測: bmm f32 12.9% + safe_softmax 2.2%（dit 内 — [op-timing-restats](research/2026-08-13-op-timing-restats.md) §3.2） | 目的は速度でなく**ポータビリティ**。加算順が変わる — ADR 0044 意味論の再導出 + 音声品質門が先行条件                                                                                                                                                                                                |
| K-6  | codec encoder（G7）のタイル化                                     | 既定 128MiB 上限機での到達可能性（[limitations](limitations.md) の encoder 節）                                                                                                                                                                             | 需要駆動のまま。decoder と同じ Uint32 ビット一致門で閉じられる見込み（halo 6 の同変性は未実測）                                                                                                                                                                                                    |
| K-7  | 融合 matcher の受理集合拡張（偶奇形 RoPE・rms_norm ベース adaLN） | Irodori DiT / speaker は融合 rope 0 / adaln 0 — 受理集合外を IR 実測で確定（融合門 `7818c12` が 0 ごと固定・網羅レビュー修正波）                                                                                                                            | 値は正しく素経路のまま = 純粋な性能候補。拡張は「式が似ていても広げない」MUST（fusion.ts）の意図的な改訂になる — 利得実測（DiT の素 RoPE/adaLN コスト）が先                                                                                                                                        |
| K-8  | attention PV（i8a8）の Vᵀ 実体化 + quantize_rows 融合             | **棄却（2026-08-13 実測）**: 対象プール ≈79ms/生成 = GPU の 1.0%・融合利得 ≈48ms = 壁の 0.45%（[op-timing-restats](research/2026-08-13-op-timing-restats.md) §2）                                                                                           | byte parity 門の設計見立て自体は正のまま — 利得が実装・保守に 1 桁足りない。K-3 と同じ**採らないが記録**                                                                                                                                                                                           |
| K-9  | SBV2 relattn 表の analytic 生成（専用 kernel）                    | ホスト生成 + H2D が 8T² bytes — T=4096 で 128MiB・Node proxy 89ms/sample（2026-08-13 外部レビュー）                                                                                                                                                         | 式は j−i と window=4 のみで kernel 生成可。表をグラフ入力に置く現設計は by-design（limitations）だが生成側の高速化は未裁定。通常 T は数百 — H-4 より後                                                                                                                                             |
| K-10 | dacvae codec-decoder convT の residue grouping                    | `conv_transpose1d:gather` が **4 dispatch / 1,707ms = irodori 全 GPU の 23.2%**（427ms/dispatch・2026-08-13 [op-timing-restats](research/2026-08-13-op-timing-restats.md) §3.2 — 単発粒度で全パイプライン最大）                                             | K-4 注記と同技法（k ≡ ox+padding (mod stride) の residue 分割 — 加算順不変のまま % / 無効 tap を除去）。**加算順不変 = f32 席の WAV sha256 門がそのまま検証器**になる見込み（数値契約を割らない側）。conv1d direct（同段 1,108ms）は implicit GEMM 案の対象                                        |

## ロード / 配布

| ID  | 候補                                                   | 根拠（出典）                                                                                                                                   | 前提・リスク                                                                                                                                                                  |
| --- | ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| L-1 | Session 構築 gap 2.50s の分解                          | op-timing-stats §9（帰属未分解の合算）                                                                                                         | 重みアップロード後の submit を消すと瞬間ピーク +2.7GiB（ACTIVE_DESIGN Pitfalls）                                                                                              |
| L-2 | EmbeddingGemma 低精度 3 系列                           | weight 1.23GB → 理論 1/2〜1/4                                                                                                                  | 品質再検証（検索順位の反転率）必須。wall 効果は H-1 の後でないと観測できない                                                                                                  |
| L-3 | hub Range 並列 + prefetch（保留）                      | 断片化物のみ並列 16 まで伸びる（[xet-fragmentation](research/2026-08-09-xet-fragmentation.md)）                                                | 公開リポは修復済みで利得が薄い — **保留のままが妥当**                                                                                                                         |
| L-4 | exporter の圧縮 serialize を全量 packed → streaming へ | write_model は圧縮重み全部を f32 と同時保持 — Irodori 級で f32 3.44GB + f16 1.72GB / i8 0.87GB の同時常駐（2026-08-13 概算）                   | header offset は dtype/shape から事前計算できる — storage plan を先に作り 1 tensor ずつ convert → exact-check → write → release へ。export 可能なモデルサイズを直接押し上げる |
| L-5 | dist の shared artifact を配置前 plan で決める         | 現行は copy → hash → `_fold_shared` で事後 dedup — shared source S × M モデルで fold 前 ≈ 3MS の論理 I/O（2026-08-13 外部レビュー）            | 配置 plan 時に共有を決め 1 回だけ materialize/hash する形へ。staging→swap 化（2026-08-12 修正波）とは独立の I/O 量の問題                                                      |
| L-6 | `_add_const` の streaming SHA 化                       | `tobytes()` + 連結で全量コピー — 64MiB proxy で ~106ms / transient ~128MiB → streaming update で ~37ms / ほぼ 0（2026-08-13 外部レビュー実測） | 純粋なビルド時間・メモリ候補。同関数の CSE キー 64bit prefix 問題（correctness）は別軸で修正予定 — 混ぜない                                                                   |
