# Codex の作業指示

このファイルは規約を複製しない。規約の正本は次の 3 つで、作業・レビューの前に毎回読み直す。

- このリポの [`CLAUDE.md`](CLAUDE.md) — プロジェクト規約（構成・検証コマンド・横断の不変条件・
  docs の置き場）。汎用規約と食い違うときはこちらを優先する。
- [`.claude/ACTIVE_DESIGN.md`](.claude/ACTIVE_DESIGN.md) — 現在の設計の焦点と落とし穴の索引。
  計画・レビューの前に必ず読む。
- `~/.claude/CLAUDE.md` と `~/.claude/rules/*.md` — 利用者の汎用規約（言語・説明の仕方・
  合意の取り方・原因調査・テスト・コミット・TypeScript）。

明示された利用者の指示は、どの規約よりも優先する。
設計の現況（manifest の版・開発機の GPU・検証の回し方など）はここへ書かず、正本を読む。
このファイルは利用者の許可により、作業方針に合わせて適宜編集してよい。

## Codex として読み替えるところ

- 正本のうち Claude 専用の実行手段（モデル名の指定・Workflow・スキル・サブエージェントへの
  委譲・メモリ）は、Codex の手段として仮定しない。
  このセッションで使える機能とその指示に従い、使っていない機能を使ったと報告しない。
- 明示された作業依頼・継続指示は、その範囲の承認として扱う。
  承認済みの段取りごとに聞き直さない。範囲を超える変更や前提の崩れは、差分と判断点を示して止まる。
- 作業ブランチは利用者の指示に従う。指示が無ければ現在のブランチで作業し、勝手に切り替えない。
  開始時とコミット前にブランチと差分を確かめ、他の作業の未コミット変更を上書きしない。
- 検証は `CLAUDE.md` の「検証コマンド」節に従う（レーン実行・環境ごとの参照値を含む）。
  GPU を使う検証とベンチを並走させない。行っていない実機確認を「確認済み」と書かない。
- React の規約は React を扱う場合だけ適用する。汎用規約を理由に依存や UI 基盤を持ち込まない。
- 最終報告は日本語で、会話履歴なしで読める形にする（やったこと / コミット一覧 / 未完のこと /
  次にやるべきこと）。

## 正本へ移すまでここに置く運用指示

利用者が Codex の作業に対して出した指示のうち、まだ正本（`CLAUDE.md` / `docs/`）に無いもの。
正本へ移したら、ここから消す。

- 実験は日時と目的を含む専用ディレクトリを新規に作り、準備スクリプト・候補コード・ログ・結果を
  その中へ保存する（置き場は [docs/assets-layout.md](docs/assets-layout.md)）。
  作成先が既に在れば失敗させる（既存を許す `mkdir` を使わない・ファイルは排他的に作る）。
  再実行も新しい出力先を使い、依存する処理は前段が成功した場合だけ走らせる。
- 利用者へベンチマークの追試を頼むときは、必要なモデル・比較対象・実行設定を比較画面の初期選択へ
  反映し、開始ボタンだけで頼んだ計測が走る形にする。選んだ内容と要る比較を説明する。
- 停止を要する想定外の問題は、データ損失・誤操作などのインシデントを指す（ADR 0097）。
  性能低下・数値誤差の増加・テスト失敗・候補の不採用は通常の調査対象で、それだけでは止まらない。
