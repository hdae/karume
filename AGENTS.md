# Codex の作業指示

グローバルの `~/.claude/CLAUDE.md`・`~/.claude/rules/*.md` と、このリポの
`CLAUDE.md`・`.claude/ACTIVE_DESIGN.md` を、Codex が作業・レビューで使う形に解釈したもの。
原文の複製ではない。明示された利用者の指示を優先し、プロジェクト規約を汎用規約より優先する。
設計の現況はこのファイルへ複製せず、正本を読み直す。
このファイルは利用者の許可により、作業方針や現状に合わせて適宜編集してよい。

## 汎用的な指示

### 合意とスコープ

- 事実の整理 → 検証可能な目標と計画 → 必要な承認 → 実装の順に進める。
  未承認のコード変更やスコープ拡張をしない。明示された作業依頼・継続指示はその範囲の承認として扱い、
  承認済みの段取りごとに聞き直さない。作業の前提は説明する。
- 複数の設計案が成立する場合は、目的・利点・欠点と推薦理由を示す。
  大きな変更は影響範囲・検証計画・段階的な分割・追加する複雑さを具体化してから判断を求める。
- 隣接問題は別件として記録し、ついでに修正しない。変更行は依頼へ追跡できる範囲に限る。
  自分の変更で不要になったコードは取り除く。既存の無関係な変更を上書きしない。
- 承認済みの仕事は検収まで継続する。前提が崩れた場合は差分と判断点を説明する。
  制限で停止するときは、作業状態・実行中コマンド・検証結果・再開手順を残す。

### 原因調査と設計

- 症状から修正を推測しない。実コード、履歴、ログ、実測から破れた不変条件を特定する。
  平滑化・閾値調整・例外の握り潰し・黙ったフォールバックを根本対策として入れない。
- 事実と推測を分け、推測は「推測」と明示する。自分と委譲先の結論も現物で反証を試みる。
  数値・件数・引用・テストの成否は実際の出力と照合し、誤りは理由とともに訂正する。
- 並行処理は実際のスケジューリング単位で考える。JS の microtask / macrotask、リース、
  リソース寿命を確認し、競合・通知の取り逃し・自己デッドロックを具体的に検討する。
- 最小限の構造で解決する。依存は一方向、責務と公開面は小さくする。
  導出できる状態を別途更新する二重管理や、未要求の設定・汎用化・互換層を足さない。
- 未対応・不正状態は fail loudly。安全な拒否を取り除いて通さない。
  コメントは理由を記し、`DECIDED:` は必ず対応する決定記録へリンクする。

### 説明・用語

- 対話・進捗・報告は日本語。結果 → 根拠 → 判断点の順に、短い文で説明する。
  読者は経験あるプログラマだが、ML・数値計算・GPU の専門家とは仮定しない。
- 略語は初出で意味を添える。一般的な用語を使い、プロジェクト内だけの「家言葉」を書かない。
  例えばスロットやイテレーションを独自の比喩へ置き換えない。識別子は現行の正式名を使う。
- 自動検証と実機の観測を区別する。行っていない操作を「手動確認済み」と書かない。
  報告は会話履歴なしで読める形にする。

### TypeScript・ツール・委譲

- `any`・`@ts-ignore` を避け、型の構造から直す。`satisfies` を優先し、不要な `as` を使わない。
  `type`、判別共用体、型ガードを使い、公開関数には戻り値の型を書く。`enum` は避ける。
- フォールバックは `??` を使い、`0`・空文字・`false` を失わない。欠如は原則 `undefined` と省略可能属性。
  エラーは例外で扱う。外部の `unknown` は境界で検証し、熱い経路で再検証しない。
  検証ライブラリをこの規約だけを理由に追加しない。
- 検索は `rg` / `rg --files` を優先する。任意 CLI の存在を確認する。
  shell 文字列はコードとして扱い、複数行の本文は引用した heredoc やファイル経由で渡す。
  formatter の変更後は現物を読み直す。
- 主担当が設計・統合・検収に責任を持つ。独立した調査・実装・レビューは境界と成果物を決めて委譲し、
  返却差分と根拠を主担当が読む。数値計算・WGSL・同期などの核心は主担当が直接扱ってよい。
  少数の操作で済む仕事は無理に分割しない。
- Claude 専用のモデル名・Workflow・スキル呼び出しは Codex の実行手段として仮定しない。
  このセッションで利用可能な機能とその指示に従い、存在しない機能を使用したと報告しない。
  React の規約は React を扱う場合のみ適用し、汎用指示を理由に依存や UI 基盤を持ち込まない。

### テストとコミット

- テストは振る舞いと不変条件を検証する。`describe` で文脈、`it` で振る舞いを表す流儀に合わせる。
  実装をなぞるだけのテストを増やさず、変更の故障を検出できる検証を選ぶ。
- 既存のアサーション・許容誤差・golden・SHA の検証条件を弱めて通さない。
  テスト自体の誤りを証明した場合は別の論理変更として扱う。同期方法の修正と期待値の変更を混同しない。
- 変更に必要な fmt・lint・型検査・テストを通し、失敗を解決してからコミットする。
  文書もリンク・構造・表記・数値の出所を検証する。
- Conventional Commits: `type(scope): 日本語の説明`。type は英語、説明は約 50 字を目安にする。
  1 コミット = 1 論理変更。本文は理由を説明し、修正では影響範囲も記す。破壊的変更は `!` を付ける。
- 調査・原因の帰属・修正・検証を作業単位で完了し、その都度コミットしてから次の独立した作業へ進む。
  複数の作業範囲の差分をまとめて溜め込まない。
- 破壊的 git 操作・worktree 削除は承認が必要。`git push` は利用者が行う。

## プロジェクト固有の指示

### 作業場所と検収

- 利用者が指定した現在のブランチで作業する。今回のレビュー対応は `codex/review-and-fix`。
  **他ブランチ、特に main には変更を加えない**。開始時とコミット前にブランチ・差分を確認する。
  worktree を追加する必要がある場合はリポ外に置く。旧作業のブランチ指定へ勝手に切り替えない。
- `models/` と `outputs/` は git 追跡外。worktree では本体リポへの symlink の場合があるため実際の配置を確認する。
  既存資産を上書きせず、計測は新しい出力先へ保存する。
  `inputs/` を含む配置は [docs/assets-layout.md](docs/assets-layout.md) に従う。
- 実験ごとに日時と目的を含む専用ディレクトリを新規作成し、準備スクリプト・候補コード・ログ・結果をその中へ保存する。
  作成先が既に存在する場合は失敗させる（`mkdir` の既存許可を使わない、ファイルは排他的作成など）。
  再実行も新しい出力先を使い、既存の実験スクリプトや結果を上書きしない。
  依存する処理は前段が成功した場合だけ実行し、準備失敗後に旧候補で計測を続けない。
- **各コミット前に `deno task verify` を通す**（fmt --check + lint + check + test）。
  実 GPU の RTX 3080 Ti を使い、GPU ベンチと GPU テストを並走させない。
  フル走行で失敗した場合はログを残し、既知の VRAM 圧の問題と断定せず、失敗ファイルの単独実行で切り分ける。
- exporter / recipe を変更した場合は `tools/exporter` と `tools/export-recipes` の両方で `uv run pytest` も通す。
  worktree をリポ内に置かない。検証対象から都合の悪いファイルを除外しない。

### アーキテクチャと不変条件

- karume は WebGPU 汎用 NN 推論スタック。Deno とブラウザで動く純 TS + WGSL を維持する。
  runtime は IR 実行、hub は資産解決・取得・cache、models はパイプラインと tokenizer を担当する。
  exporter の汎用 core とモデル固有 recipe の境界を維持する。
- runtime の公開 API は `mod.ts` の薄い面に限る。models は barrel とファミリ別サブパスを両立する。
- **全モジュール副作用ゼロ**。top-level 登録、グローバル可変状態、import に伴う処理を禁止する。
  `GPUBufferUsage` などの WebGPU グローバルをモジュールスコープで読まない。
  runtime では `src/gpu/webgpu-constants.ts` を使う。
- ランタイム依存は Web 標準 API で構成されたものだけ。相対 import は **`.ts` 付き**。
  WGSL はテンプレートリテラル、TypedArray は `<ArrayBuffer>` を明示する。
- 同じ codegen キーからはバイト単位で同じ WGSL を生成する。elementwise / 行 reduce は grid-stride 前提。
  compute 系も `requiredLimits` に含め、validation error scope を維持する。
  シンボル束縛は `Object.hasOwn` で検査する。
- バッファは flush-before-destroy。Session 構築時の重み転送後 submit はメモリピークを抑えるため維持する。
  借用 state / weight の寿命、run リース、deferred commit の条件は ADR と実コードで確認する。
- op ごとの数値許容値は `packages/runtime/tests/helpers/op-tolerance.ts` が正本。未登録 op を既定値で通さない。
  codegen snapshot・golden・SHA・ビット同一の検証を維持する。
  Metal 固有の書き込み展開や丸め障壁を安易に簡略化しない。
- 公開 revision はファミリごとの `<FAMILY>_SOURCES` が正本。SHA を docs やテストへ複製しない。
  manifest は `karume/4`。未対応形式を互換処理で黙って受理しない。
  公開済みパッケージに、未リリース前提の破壊的変更許可を適用しない。

### 文書と用語の正本

- **README とモデルカードは英語**。コードコメント・docstring・`docs/`・ACTIVE_DESIGN・コミット説明は日本語。
  実際のエラー文の引用、日本語 TTS デモ入力は翻訳しない。
- 先行実験プロジェクトは「プロトタイプ」と呼ぶ。その固有名・旧名をコード、コメント、文書、
  コミットメッセージなどの成果物へ書かない。改名済みの識別子も新規成果物では現行名を使う。
- 計画・レビュー前に `CLAUDE.md`、`.claude/ACTIVE_DESIGN.md`、関連 ADR と現物を読む。
  ACTIVE_DESIGN は現在の焦点と落とし穴の索引であり、履歴の保存先ではない。
- 文書の役割を混ぜない。
  - `docs/decisions/`: 設計判断の理由。`DECIDED:` のリンク先。
  - `docs/backlog.md`: 作業順・項目・状態の正本。
  - `docs/perf-ledger.md`: 性能候補・採否・打ち切り条件。数値の正本は research。
  - `docs/research/`: 調査と実測の時点スナップショット。冒頭にその性格を示す 1 行を置く。
  - `docs/known-issues.md` / `docs/limitations.md`: 未解決バグ / 意図的な制約。
  - `docs/ir-v2.md` / `docs/op-vocabulary.md`: IR 仕様 / op 語彙。
  - `docs/assets-layout.md` / `docs/release-runbook.md`: 資産配置 / 恒久的な公開手順。

### レビュー後の改善調査

- 再開時は [調査と引き継ぎ](docs/research/2026-09-10-codex-mtp-optimization.md#分割コミットと再開用の引き継ぎ) を読み、
  修正済みの項目と未完項目を区別する。計測 → 帰属 → 対策の提案の順を守る。
  小さな修正は承認済みの範囲で実装してよい。データ損失・誤操作・権限外の変更など、
  被害の発生や拡大が懸念されるインシデントでは関連作業を止め、状況を保存して指示を仰ぐ。
  性能低下・数値誤差の増加・テスト失敗・実験候補の不採用は通常の調査対象であり、それだけで停止しない。
  原因の切り分けと改善・採否判断を継続し、検証条件を弱めたり不一致を黙って受理したりしない。
- このレビュー作業のサブエージェント利用は序盤の棚卸しまで。以降の調査・修正は主担当で行う。
- 利用者へベンチマークの追試を依頼するときは、その時点で必要なモデル・比較対象・実行設定を画面の初期選択へ反映する。
  原則として開始ボタンだけで依頼した計測を実行できるようにし、選択内容と必要な比較を説明する。
  全パターンを毎回要求せず、参照経路や全組み合わせの選択肢は残す。
- MTP は Gemma 4 公式 drafter を使う投機デコード。MTP を扱う場合は ADR 0096 と引き継ぎで指定された research・生データを順に読む。
- 公開 API・IR・保存形式・数値契約・既定動作の変更は、変更前後と互換性への影響を実装前に説明し、ADR へ記録する。
  大きなモジュール変更やカーネル追加も、目的・影響範囲・検証計画・段階分割を具体化してから着手する。
  利用者が承認した統合案・作業範囲に含まれる実装は継続し、段階ごとに同じ承認を求め直さない。
  再確認するのは、承認範囲を超える仕様変更・新たな破壊的変更、または上記のインシデントが判明した場合。
  この規則の目的は無説明の仕様変更を防ぐことであり、承認済みの改善や新カーネルの実装を一律に禁止することではない。
  副作用ゼロと、同じ実行設定での MTP verify 行 0 / decode の u32 一致を守る。
- 数値一致は比較検証を容易にするための参照経路で保証する。数値を変える高速化は runtime / カーネルでは
  個別の任意指定として追加し、検証済みの参照経路・golden・明示的な戻し方を維持する。
  高速化は quant 定義の session に明示し、検証済みの高速化付き quant を defaultQuant に選ぶ
  （利用者方針 2026-09-13）。モデルコードがサイズや形状から並列設定を自動で足す方式にはしない。
  明示した実行設定を quant の値より優先し、参照用 quant も選べるようにする。
  token 列や下位 bit が異なることだけを不採用理由にせず、大幅な品質劣化の有無と性能を評価する。
  採否判断を委任された範囲では調査・生成結果の確認から判断して進め、未検証のモデルへ一括適用しない。
  モデル既定の変更と参照 golden の更新を同一視しない。参照値はその設定を明示して保持し、
  モデル既定の適用と既定/明示指定の対応を別に検証する。理由・範囲・残る品質制限は ADR へ記録する。
- 結果は `docs/research/2026-09-10-codex-mtp-optimization.md` に追記する。
  数値は保存した JSONL などの実測へ辿れる形にし、推測と実測を区別する。
  Apple M2 はここでは実行できない。RTX の結果を M2 の実測として扱わない。
- 最終報告は日本語で「やったこと / コミット一覧 / 未完のこと / 次にやるべきこと」を含める。
